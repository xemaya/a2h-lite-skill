# Hermes platform config — sourced by scripts/write-mcp-config.sh
#
# TODO (beta-user calibration): confirm the real MCP config file path
# and restart command on a live Hermes install, and replace these
# placeholders.

MCP_CONFIG_PATH="${MCP_CONFIG_PATH:-$HOME/.hermes/mcp.json}"
MCP_SERVER_NAME="a2h"
MCP_URL="https://api.a2hmarket.ai/a2hmarket-concierge/mcp/sse"
RESTART_CMD="hermes mcp reload"
