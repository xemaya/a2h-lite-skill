# Claude Code platform config — sourced by scripts/write-mcp-config.sh
#
# Default to the 2026-era path (~/.claude.json).  Older CC versions stored
# MCP server entries in ~/.claude/mcp_servers.json — override MCP_CONFIG_PATH
# in your environment if you are on an older CLI.

MCP_CONFIG_PATH="${MCP_CONFIG_PATH:-$HOME/.claude.json}"
MCP_SERVER_NAME="a2h"
MCP_URL="https://api.a2hmarket.ai/a2hmarket-concierge/mcp/sse"
RESTART_CMD="claude mcp restart a2h"
